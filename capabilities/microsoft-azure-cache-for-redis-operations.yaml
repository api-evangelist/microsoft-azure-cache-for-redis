naftiko: 1.0.0-alpha2
info:
  label: Azure Cache for Redis REST API — Operations
  description: 'Azure Cache for Redis REST API — Operations. 1 operations. Lead operation: Azure Cache for Redis REST API
    List operations. Self-contained Naftiko capability covering one Microsoft Azure Cache For Redis business surface.'
  tags:
  - Microsoft Azure Cache For Redis
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_CACHE_FOR_REDIS_API_KEY: MICROSOFT_AZURE_CACHE_FOR_REDIS_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-cache-for-redis-operations
    baseUri: https://management.azure.com
    description: Azure Cache for Redis REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.Cache-operations
      path: /providers/Microsoft.Cache/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Cache for Redis REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_CACHE_FOR_REDIS_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-cache-for-redis-operations-rest
    port: 8080
    description: REST adapter for Azure Cache for Redis REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-cache/operations
      name: providers-microsoft-cache-operations
      description: REST surface for providers-Microsoft.Cache-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Cache for Redis REST API List operations
        call: microsoft-azure-cache-for-redis-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-cache-for-redis-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Cache for Redis REST API — Operations. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: azure-cache-redis-rest-api
      description: Azure Cache for Redis REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-cache-for-redis-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
